# معماری سیستم مدیریت اهدای خون

## معرفی

این پروژه یک سامانه مدیریت اهدای خون است که با استفاده از **Django** و **Django REST Framework** پیاده‌سازی شده است.

هدف سیستم، ایجاد ارتباط بین مراکز درمانی و اهداکنندگان خون است تا فرآیند ثبت درخواست خون، ثبت‌نام اهداکنندگان و مدیریت وضعیت درخواست‌ها به صورت ایمن و قابل اطمینان انجام شود.

---

# معماری کلی سیستم

سیستم از معماری چندلایه (Layered Architecture) استفاده می‌کند.

```
                 +-----------------------+
                 |      Client           |
                 | (وب یا موبایل)        |
                 +-----------+-----------+
                             |
                         REST API
                             |
                 +-----------v-----------+
                 |    View (DRF API)     |
                 +-----------+-----------+
                             |
                       Serializer
                             |
                 +-----------v-----------+
                 | Business Logic(Model) |
                 +-----------+-----------+
                             |
                       Database
```

در این معماری:

- View مسئول دریافت درخواست‌ها و بررسی سطح دسترسی کاربران است.
- Serializer داده‌های ورودی و خروجی را اعتبارسنجی می‌کند.
- Model قوانین اصلی سیستم و منطق کسب‌وکار را پیاده‌سازی می‌کند.
- پایگاه داده وظیفه نگهداری اطلاعات را بر عهده دارد.

---

# نمودار ERD

```
                 +-------------+
                 |    User     |
                 +-------------+
                        |
        +---------------+----------------+
        |                                |
        |                                |
+------------------+        +-------------------------+
|  DonorProfile    |        | MedicalStaffProfile     |
+------------------+        +-------------------------+
        |                                |
        |                                |
        |                        +------------------+
        |                        | MedicalCenter    |
        |                        +------------------+
        |                                 |
        |                                 |
        |                         +------------------+
        |                         | BloodRequest     |
        |                         +------------------+
        |                                 |
        +---------------+-----------------+
                        |
                  +--------------+
                  |  Donation    |
                  +--------------+
```

---

# توضیح موجودیت‌ها

## User

اطلاعات ورود کاربران را نگهداری می‌کند.

هر کاربر دارای یکی از نقش‌های زیر است:

- اهداکننده
- کادر درمان
- مدیر مرکز درمانی

---

## DonorProfile

اطلاعات شخصی اهداکنندگان را نگهداری می‌کند.

هر اهداکننده:

- متعلق به یک User است.
- می‌تواند چندین Donation داشته باشد.

---

## MedicalStaffProfile

اطلاعات کارکنان مراکز درمانی را نگهداری می‌کند.

هر کارمند:

- متعلق به یک User است.
- به یک MedicalCenter وابسته است.

---

## MedicalCenter

نماینده بیمارستان یا مرکز درمانی است.

هر مرکز درمانی دارای:

-تعدادی کارمند
- درخواست های خون دادن

است.

---

## BloodRequest

نماینده یک درخواست خون است.

فیلدهای مهم:

- گروه خونی
- ظرفیت کل
- ظرفیت باقی‌مانده
- وضعیت درخواست

هر درخواست متعلق به یک مرکز درمانی است.

---

## Donation

ثبت‌نام یک اهداکننده برای یک درخواست خون را نمایش می‌دهد.

وضعیت‌های آن عبارت‌اند از:

- Pending
- Donated
- Cancelled
- Expired

---

# مدیریت سطوح دسترسی

برای احراز هویت از JWT استفاده شده است.

## اهداکننده

امکان انجام عملیات زیر را دارد:

- مشاهده درخواست‌های فعال
- ثبت‌نام برای اهدای خون
- مشاهده سوابق اهدای خود
- لغو درخواست اهدای خود

امکان انجام عملیات زیر را ندارد:

- ایجاد درخواست خون
- مدیریت درخواست‌ها
- تغییر وضعیت اهدا

---

## کادر درمان

امکان انجام عملیات زیر را دارد:

- ایجاد درخواست خون
- ویرایش درخواست
- مشاهده اهداکنندگان
- لغو ثبت اهدا
- ثبت وضعیت «اهدای انجام شد»
- پایان دادن به درخواست خون

---

## مدیر مرکز درمانی

وظیفه بررسی و تأیید درخواست ثبت‌نام کارکنان مرکز درمانی را بر عهده دارد.

---

# مدیریت همزمانی (Concurrency Management)

یکی از مهم‌ترین بخش‌های این پروژه جلوگیری از وقوع **Race Condition** هنگام ثبت‌نام اهداکنندگان است.

## سناریو

فرض کنید ظرفیت باقی‌مانده یک درخواست خون برابر با ۱ باشد.

در همان لحظه دو اهداکننده به طور همزمان دکمه «اعلام آمادگی» را فشار دهند.

اگر هیچ مکانیزمی برای همزمانی وجود نداشته باشد، هر دو درخواست مقدار ظرفیت را برابر ۱ مشاهده می‌کنند و هر دو ثبت خواهند شد.

نتیجه:

- ظرفیت منفی می‌شود.
- تعداد ثبت‌نام‌ها بیشتر از ظرفیت خواهد شد.
- داده‌های پایگاه داده ناسازگار می‌شوند.

---

# روش پیاده‌سازی

برای جلوگیری از این مشکل از **Pessimistic Locking** استفاده شده است.

در View مربوط به ثبت اهدا، عملیات داخل یک تراکنش دیتابیس انجام می‌شود:

```python
transaction.atomic()
```

سپس رکورد مربوط به درخواست خون با استفاده از:

```python
select_for_update()
```

قفل می‌شود.

به این ترتیب:

1. اولین درخواست، رکورد را قفل می‌کند.
2. درخواست دوم تا پایان تراکنش منتظر می‌ماند.
3. درخواست اول ظرفیت را کاهش می‌دهد.
4. تراکنش Commit می‌شود.
5. درخواست دوم دوباره مقدار ظرفیت را بررسی می‌کند.
6. اگر ظرفیت صفر باشد، ثبت‌نام انجام نمی‌شود.

به این ترتیب امکان ثبت بیش از ظرفیت وجود نخواهد داشت.

---

# روند ثبت اهدا

```
اهداکننده
      |
      | درخواست ثبت اهدا
      |
      v
شروع Transaction
      |
قفل کردن BloodRequest
      |
بررسی ظرفیت
      |
ایجاد Donation
      |
کاهش remaining_capacity
      |
Commit
```

---

# مدیریت ظرفیت درخواست

هنگام ایجاد درخواست خون:

```
remaining_capacity = total_capacity
```

پس از ثبت موفق هر اهداکننده:

```
remaining_capacity -= 1
```

اگر ظرفیت به صفر برسد:

```
status = Pending
```

در صورت لغو ثبت اهدا:

```
remaining_capacity += 1
```

اگر وضعیت درخواست Pending باشد:

```
status = Active
```

---

# حفظ یکپارچگی داده‌ها

برای جلوگیری از ورود داده‌های نامعتبر، چندین لایه اعتبارسنجی استفاده شده است.

## اعتبارسنجی مدل

در تمامی مدل‌ها از:

```
full_clean()
```

استفاده شده است.

نمونه قوانین:

- ظرفیت باقی‌مانده نباید از ظرفیت کل بیشتر باشد.
- گروه خونی قابل تغییر نیست.
- ظرفیت کل قابل کاهش نیست.
- وضعیت Donation باید معتبر باشد.

---

## محدودیت‌های پایگاه داده

برای جلوگیری از ثبت تکراری، از Unique Constraint استفاده شده است.

هر اهداکننده فقط یک Donation فعال برای هر درخواست خون می‌تواند داشته باشد.

---

## تراکنش‌های دیتابیس

تمام عملیات حساس مانند ثبت اهدا داخل:

```
transaction.atomic()
```

اجرا می‌شوند.

در صورت بروز خطا، تمامی تغییرات Rollback شده و پایگاه داده در وضعیت صحیح باقی می‌ماند.

---

## قفل‌گذاری رکورد

برای جلوگیری از Race Condition از:

```
select_for_update()
```

استفاده شده است.

این روش باعث می‌شود در زمان ثبت همزمان چند درخواست، فقط یک تراکنش بتواند رکورد درخواست خون را تغییر دهد.

---

# ایندکس‌های پایگاه داده

برای افزایش سرعت جستجو، روی فیلدهای پرکاربرد ایندکس تعریف شده است.

در BloodRequest:

- status
- blood_group

در Donation:

- status

---

# طراحی API

تمام APIها مطابق اصول REST طراحی شده‌اند.

نمونه‌ای از Endpointها:

```
GET    /requests/

GET    /requests/{id}/

POST   /requests/create/

PATCH  /requests/{id}/update/

POST   /requests/{id}/donate/

PATCH  /donations/{id}/

POST   /requests/{id}/resolve/
```

---

# قابلیت توسعه

معماری پروژه به گونه‌ای طراحی شده است که در آینده بتوان امکانات زیر را به آن اضافه کرد:

- ارسال اعلان به اهداکنندگان
- زمان‌بندی خودکار انقضای درخواست‌ها
- استفاده از Redis برای کش
- پردازش‌های پس‌زمینه با Celery
- استقرار روی PostgreSQL برای پشتیبانی کامل از مدیریت همزمانی
